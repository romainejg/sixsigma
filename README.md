# sixsigma

This repository contains the CSSGB Streamlit learning app in `/cssgb_exam_app`.

Run locally:

```bash
cd cssgb_exam_app
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
